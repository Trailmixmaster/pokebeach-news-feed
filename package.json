{
  "name": "pokebeach-news-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that scrapes PokéBeach news and posts to Discord via webhook",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "firebase-admin": "^13.6.1",
    "node-fetch": "^3.3.2",
    "node-html-parser": "^6.1.13"
  },
  "devDependencies": {
    "wrangler": "^4.66.0"
  }
}
